name: Context Examples

on: 
  push:
    branches:
      - main

jobs:
  my-context:
    runs-on: ubuntu-latest
    steps:
      
      - name: step1
        run: |
          echo "Hello! $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }}
     