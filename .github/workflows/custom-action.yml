on: [push]


jobs:
  hello-johann:
    runs-on: ubuntu-latest
    steps:
      - name: Hellow World action
        id: hello
        uses: j4rmenkell/barsoom@main
        with:
          who-to-greet: 'AYOKO NA'
      - name: Get the Output
        run: echo "The time was ${{ steps.hello.outputs.greeting }}"
